
    <div class="header bg-secondary-subtle mb-4 p-4 rounded-3 text-center">
        <h1>Please provide feedback. You may do so anonymously.</h1>
    </div>

    {{!-- Bootstrap row container - creates a horizontal row for columns --}}
    <div class="row">

        {{!-- First column - Instructions section 
             col-md-5: takes up 5/12 of the row width on medium screens and larger --}}
        <div class="col-md-5">  
            <h2>Instructions</h2>

            <p>As one of our students, your thoughts, feedback and comments are important to us.</p>
            <p>Please send them using this form.</p>
            <p>If you would like us to follow up with you, include your contact information.</p>
        </div>   <!-- end of first instructions col div -->

        {{!-- Second column - Form section
             col-md-7: takes up 7/12 of the row width on medium screens and larger --}}
        <div class="col-md-7">
            {{!-- Form element with POST method - data will be sent to the server
                 action="/submit-feedback": the URL where form data will be submitted --}}
            <form method="POST" action="/submit-feedback">

                {{!-- Name field (optional) --}}
                <label class="form-label" for="name">Name</label>
                {{!-- form-control: Bootstrap class for styling form inputs --}}
                <input class="form-control" id="name" name="name">
                <br>

                {{!-- Email field (optional) with type="email" for validation --}}
                <label class="form-label" for="email">email</label>
                <input class="form-control" id="email" type="email" name="email">
                <br>

                {{!-- Comments field (required) - note the required attribute --}}
                <label class="form-label" for="comments">Comments</label>
                {{!-- textarea creates a multi-line text input field --}}
                <textarea class="form-control" id="comments" name="comments" required></textarea>
                <br>

                {{!-- Radio button section for current student status --}}
                <p class="mx-2">Are you a current student?</p>

                {{!-- Radio button for "Yes" option
                     form-check: Bootstrap class for styling radio buttons and checkboxes --}}
                <div class="form-check">
                    {{!-- Both radio buttons have the same name ("current-student") so only one can be selected --}}
                    <input class="form-check-input" id="yes" type="radio" value="yes" name="current-student">
                    <label class="form-check-label" for="yes">Yes</label>
                </div>

                {{!-- Radio button for "No" option --}}
                <div class="form-check">
                    <input class="form-check-input" id="no" type="radio" value="no" name="current-student">
                    <label class="form-check-label" for="no">No</label>
                </div>
                <br>

                {{!-- Submit button with Bootstrap styling
                     btn btn-primary: creates a blue button --}}
                <input class="btn btn-primary" type="submit">

            </form>
        </div>  <!-- end of second form col div -->

    </div>  <!-- end of the row div that contains both columns -->
